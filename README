# Source code config
SSID, PWD and IP_ADDRESS should be defined before flashing (wifi.c)

# Building config
Use "$ west build -p always -b disco_l475_iot1" to build the project with non-secure communication
and "$ west build -p always -b disco_l475_iot1 -- -DCONF_FILE="prj.conf overlay-tls.conf"" to build with secure communcation





